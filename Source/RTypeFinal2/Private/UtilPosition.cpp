#include "UtilPosition.h"

UUtilPosition::UUtilPosition() {
}

FVector UUtilPosition::TransVectorWorldToScreenActor(UObject* _worldContext, FVector _wnml) {
    return FVector{};
}

FVector UUtilPosition::TransVectorScreenActorToWorld(UObject* _worldContext, FVector _snml) {
    return FVector{};
}

FRotator UUtilPosition::TransRotationWorldToScreenActor(UObject* _worldContext, FRotator _wpos) {
    return FRotator{};
}

FRotator UUtilPosition::TransRotationScreenActorToWorld(UObject* _worldContext, FRotator _spos) {
    return FRotator{};
}

FVector UUtilPosition::TransPositionWorldToScreenActor(UObject* _worldContext, FVector _wpos) {
    return FVector{};
}

FVector UUtilPosition::TransPositionScreenActorToWorld(UObject* _worldContext, FVector _spos) {
    return FVector{};
}


